span Index
ul(ng-repeat="x in galeries")
  li
    span {{x.title}}
    span :
    span {{x.description}}
    br
    a(ng-show="logged" ng-click="delete(x.id)") Delete
    a(ui-sref="galerie.show({id:x.id})") Show
    a(ng-show="logged" ui-sref="galerie.edit({id:x.id})") Edit
pagination.pagination-sm(total-items="totalItems" ng-model="currentPage"
ng-change="pageChanged()" previous-text="Précédent" items-per-page="5"
next-text="Suivant")
